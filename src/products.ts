export interface Product {
    name: string;
    price: number;
}

const products = ["lure", "pole", "tackle box"];

function calcAverageProductPrice(products = []) {
    let total = 0;
    for(let i = 0; i < products.length; i++){
        total += products[i];
    } if(products = ){
        return total += products[i];
    } else(products = [])
        return 0;
    };


export default calcAverageProductPrice(products){
    console.log("lure", "pole", "tackle box")
}